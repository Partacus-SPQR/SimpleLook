# Mod Properties
mod.id=simplelook
mod.name=SimpleLook
mod.version=1.0.0

# Gradle JVM Settings
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m

# Shared Dependencies (used across all versions)
deps.fabric_loader=0.18.2
